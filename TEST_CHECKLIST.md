# 预约服务小程序发布核对清单

所有复选框都必须依据实际验证结果填写。自动化检查通过不能替代云端配置、真机和公众平台验收。

## 一、用户端关键流程

| # | 验证项 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| 1 | 打开首页 | 页面加载完成，无当前错误，展示已发布的运营配置 | ☐ |
| 2 | 查看联系入口 | 仅在已配置真实电话时显示并可拨号 | ☐ |
| 3 | 打开位置 | 仅使用已发布坐标；未配置时提示“暂无门店位置信息” | ☐ |
| 4 | 打开“服务”Tab | 显示近期日期状态和可用服务 | ☐ |
| 5 | 点击休息、停业或约满日期 | 不进入下一步，并显示不可预约原因 | ☐ |
| 6 | 选择可预约日期 | 日期高亮并进入服务选择 | ☐ |
| 7 | 选择服务 | 保持单选，价格、时长和服务名称正确 | ☐ |
| 8 | 加载可用时段 | 仅显示满足权威服务时长的可用时段 | ☐ |
| 9 | 选择时段并确认 | 创建一条预约并显示成功提示 | ☐ |
| 10 | 重复提交同一请求 | 返回首次预约，不产生重复记录 | ☐ |
| 11 | 打开“我的预约” | 列表按状态显示本人预约 | ☐ |
| 12 | 查看预约详情 | 日期、时段、服务、6 位码和二维码均正确 | ☐ |
| 13 | 取消未开始预约 | 状态变为已取消，可用性同步恢复 | ☐ |
| 14 | 取消已开始或过期预约 | 请求被拒绝，原状态不变 | ☐ |
| 15 | 查看已完成预约 | 状态和核销时间正确 | ☐ |
| 16 | 打开用户协议 | 文案与实际服务、运营主体一致 | ☐ |
| 17 | 打开隐私保护指引 | 优先打开平台隐私合同，回退文案可读 | ☐ |
| 18 | 首次手机号登录 | 登录成功并建立用户档案 | ☐ |
| 19 | 已有用户再次登录 | 不重复建档，历史预约仍可见 | ☐ |
| 20 | 注销无待处理预约的账号 | 个人资料删除、历史预约匿名化并退出登录 | ☐ |
| 21 | 注销仍有待处理预约的账号 | 请求被拒绝并提示先取消预约 | ☐ |

## 二、工作人员流程

| # | 验证项 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| 22 | 普通用户进入工作台 | 服务端拒绝访问 | ☐ |
| 23 | 工作人员打开工作台 | 仅显示北京时间当天预约和必要字段 | ☐ |
| 24 | 输入正确 6 位核销码 | 仅匹配当天待处理预约并完成核销 | ☐ |
| 25 | 扫描正确二维码 scene | 仅匹配对应当天待处理预约并完成核销 | ☐ |
| 26 | 核销未来日期预约 | 请求被拒绝，预约状态不变 | ☐ |
| 27 | 手输码或跨字段发生碰撞 | 请求被拒绝，不任选任一预约 | ☐ |
| 28 | 核销成功 | 权限复核、服务快照、提成记录和完成状态原子写入 | ☐ |
| 29 | 提成写入失败 | 整个事务回滚，预约仍为待处理 | ☐ |
| 30 | 重复核销同一预约 | 不产生重复提成记录 | ☐ |

## 三、管理后台

| # | 验证项 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| 31 | 密码登录 | 正确凭据成功，错误凭据统一失败且受持久化限流保护 | ☐ |
| 32 | 微信扫码登录（启用时） | 已配置时绑定管理员成功、其他账号被拒绝；未配置时入口隐藏且密码登录可用 | ☐ |
| 33 | 角色权限 | 页面权限与云函数服务端权限一致 | ☐ |
| 34 | 预约管理 | 列表、筛选、详情和日期格式正确 | ☐ |
| 35 | 服务管理 | 新增、编辑、禁用和删除均符合权限 | ☐ |
| 36 | 工作人员管理 | 新增、编辑、启停和微信绑定正常 | ☐ |
| 37 | 提成配置与统计 | 零值、金额和筛选结果均正确，无 `NaN` | ☐ |
| 38 | 客户管理 | 搜索、备注和黑名单操作正确 | ☐ |
| 39 | 营业配置草稿 | 保存草稿不会影响已发布配置 | ☐ |
| 40 | 发布营业配置 | 真实电话、地址、坐标通过校验后才可发布 | ☐ |
| 41 | 配置版本与回滚 | 历史版本可查看并回滚到选定版本 | ☐ |
| 42 | 初始化安全内容 | 仅超级管理员可执行，重复执行保持幂等 | ☐ |
| 43 | 资讯管理 | 草稿不公开，受限内容不能发布 | ☐ |
| 44 | 停业与休假管理 | 新增、删除和导入操作正确 | ☐ |
| 45 | 审计日志 | 关键管理操作包含管理员、动作、结果和时间 | ☐ |

## 四、仓库与构建门禁

| # | 验证项 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| 46 | 业务 JavaScript 语法检查 | 全部通过 | ☑ |
| 47 | JSON 与云函数锁文件检查 | 全部可解析，发布函数均有锁文件 | ☑ |
| 48 | `node --test tests/*.test.cjs` | 全部通过 | ☑ |
| 49 | `admin-web` 生产构建 | 构建成功，无阻断警告 | ☑ |
| 50 | `git diff --check` | 无空白错误 | ☑ |
| 51 | 微信开发者工具编译 | 当前错误 0、问题 0 | ☑ |

## 五、云端与公众平台门禁

- [ ] 已在目标云环境部署 `cloudbaserc.json` 中全部云函数，并记录各自版本和部署时间。
- [ ] 已按兼容顺序先部署预约相关云函数，验证旧前端/新云函数和新前端/旧云函数的过渡行为。
- [x] 已创建并通过 2026-08-30 云端索引元数据逐项验证固定复合索引矩阵：
  - `appointments(date asc, status asc, start_time asc)`
  - `appointments(verify_code asc, status asc)`
  - `appointments(verify_code asc, date asc, status asc)`
  - `appointments(qr_scene asc, status asc)`
  - `appointments(qr_scene asc, date asc, status asc)`
  - `appointments(patient_openid asc, created_at desc)`
  - `appointments(patient_openid asc, status asc, created_at desc)`
  - `users(status asc, created_at desc)`
  - `technicians(openid asc, status asc)`
  - `technicians(phone asc, status asc)`
  - `technicians(status asc, created_at desc)`
  - `services(status asc, sort_order asc)`
  - `articles(status asc, sort_order asc)`
  - `holidays(date asc, type asc)`
  - `holidays(type asc, date asc)`
  - `tech_days_off(technician_id asc, date asc)`
  - `commission_records(technician_id asc, date asc, created_at desc)`
  - `business_config_versions(created_at desc)`
  - `admin_users(role asc, status asc)`
  - `admin_sessions(admin_user_id asc, status asc)`
  - `login_sessions(status asc, expires_at asc)`
  - `admin_audit_logs(created_at desc)`
- [ ] 已确认目标环境保持 CloudBase 默认单字段索引启用；如已关闭，按源码中的单字段筛选和排序逐项补齐。
- [ ] 已逐项验证后台实际使用的客户、预约、提成和审计动态筛选组合；遇到 CloudBase 索引提示时按实际等值字段与排序字段补齐复合索引并复测分页。
- [ ] 已用隔离数据验证未来预约不可提前核销、冲突码拒绝、提成失败事务回滚和并发预约互斥。
- [ ] 已通过云开发控制台预置至少一个启用的 `super_admin`，确认代码无自动创建路径，并删除历史 `ADMIN_BOOTSTRAP_USERNAME`、`ADMIN_BOOTSTRAP_PASSWORD` 环境变量。
- [ ] 已确认所有 `admin_users.password_hash` 均为 `scrypt$...`；如仍有旧哈希，仅在迁移窗口配置原 `ADMIN_PASSWORD_SALT`，完成登录迁移后立即删除该环境变量。
- [x] 已执行管理后台和云函数生产依赖审计；管理后台无已知风险，13 个云函数的上游风险按下一项限时接受。
- [x] `wx-server-sdk 4.0.2` 仍有 5 个高危和 1 个中危上游风险。负责人：项目负责人（本次发布授权人）；有效期：2026-08-30 至 2026-11-28。当前调用图不可达相关风险入口，契约测试禁止 `.watch()`、`requestClient`、动态 OpenAPI、自定义服务地址/代理及请求对象直接入库；到期前必须复审并升级或重新授权。此项为“风险接受”，不表示“漏洞已修复”。
- [ ] 已确认正式运行前不再依赖到期型体验环境；标准版不在本次范围，若使用个人版，已补齐独立备份、长期日志、告警和恢复演练。
- [ ] 已确认手机号组件的主体资格、体验额度/余额和失败路径可用，且隐私说明与实际调用一致。
- [ ] 已开启云开发匿名登录，并配置云函数调用权限、Web 安全域名和小程序合法域名。
- [ ] 已在云函数安全规则中将 `sendReminder.invoke` 设为 `false`；已验证小程序和 Web 客户端无法直接调用，`reminderTrigger` 定时触发器仍可执行。
- [ ] 已为生产管理后台绑定自有 HTTPS 域名，并通过 CloudBase HTTP 网关配置和实测 `Content-Security-Policy`、`Strict-Transport-Security`、`X-Content-Type-Options`、`Referrer-Policy`、`Permissions-Policy` 及防嵌入策略。
- [ ] 已验证 HTML 使用短缓存、带内容哈希的静态资源使用一年长缓存；部署后已刷新 CDN，并确认线上入口与本次构建资源哈希一致。
- [ ] 如对外承诺订阅通知，已确认模板、权限、模板 ID、失败/待确认处理和双平台真机结果；如不启用，已记录模板为空、界面无承诺及双平台 N/A 禁用证据。
- [ ] 已按 14 个正式集合清单使用 JSON 全字段导出到独立账号或独立地域的加密对象存储，并在独立 `RECOVERY_ENV_ID` 完成合成集合恢复；已核对 SHA-256、记录数、索引、PRIVATE 权限、实际 RPO/RTO、失败告警和保留策略。
- [ ] 已开启 CloudBase 日志监控，覆盖 13 个云函数的运行日志和服务调用日志并保留至少 30 天；已用 `admin.health` 验证一次可检索调用，并用临时“5 分钟内调用次数 ≥ 1”规则验证告警接收人和通知渠道。当前控制台显示日志未开启时不得勾选。
- [ ] `.github/workflows/production-monitor.yml` 已推送到默认分支并每 15 分钟验证管理后台静态入口；未绑定自有域名时使用 CloudBase 官方托管入口，绑定后仓库变量 `PRODUCTION_ADMIN_URL` 已指向正式入口；负责人已启用 Actions 失败通知，并通过手动 `force_failure` 留有一次成功和一次受控失败证据。
- [ ] 已确认已发布的运营主体、名称、联系电话、地址、坐标和 POI 真实一致，并现场验证拨号与导航。
- [ ] 已在公众平台完成隐私保护指引、用户协议、个人信息处理说明及相关权限声明。
- [ ] 已确认小程序名称、简介、服务类目、主体资料和审核截图与实际服务一致。
- [ ] 已部署管理后台静态资源，并用无痕窗口完成登录和核心页面冒烟。
- [ ] 已在至少一台 iOS 和一台 Android 真机完成用户预约、取消、工作人员核销和账号注销。
- [ ] 已记录提交版本、配置变更、云函数版本和回滚版本，并验证上一版构建产物可恢复。
- [ ] 已上传小程序版本并完成提交审核前的最终复核。

## 六、发布版本与回滚记录

部署前填写一行；云函数应记录 13 个函数的实际版本或部署批次，不得只写“已部署”。

| 发布批次 | Git SHA | 管理后台入口资源 | 云函数版本/部署时间 | 数据库与索引变更 | 上一可回滚版本 | 操作人 | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-30-31f6954 | `31f6954281e466b7d2f785c49c276c9b2676537d` | `index-C7HXq61_.js`；`dist/index.html` SHA-256 `6a02f4288e954d64d161d1fa7b92ad5899796a03ed2231af93623618b3e8233f` | `admin`、`login`、`createAppointment` 已部署为 `$LATEST`，并发布不可变版本 `3`；其余 10 个函数沿用当前线上版本 | 无 | 三个已部署函数版本 `2`；管理后台旧入口 `index-BIOFdWwT.js`；`/private/tmp/yxt-admin-pre-31f6954.EoolXf` 仅为当次临时快照，不作为当前或未来回滚证据 | Codex | 部署完成；线上入口命中新构建，登录页可加载；扫码登录仍因既有小程序码配置或云调用权限缺口失败，部署前版本 `2` 复测结果相同；不满足正式上架门禁 |
| 2026-08-30-8d34af5 | `8d34af53a16c2f274d877c10e6bb5dda21d5fedf` | 沿用 `index-C7HXq61_.js`；`dist/index.html` SHA-256 `6a02f4288e954d64d161d1fa7b92ad5899796a03ed2231af93623618b3e8233f` | `admin` 仅更新代码并发布不可变版本 `4`（2026-08-30 19:18:35）；其余 12 个函数未变；`$LATEST` 保持 100% | 无结构或索引变更；隔离扫码会话按失败路径过期 | `admin` 版本 `3` | Codex | 未知 action 加载探针成功；正式后台扫码入口仍失败。配置仅有 `ADMIN_PASSWORD_SALT`、`WECHAT_MINIPROGRAM_QR_ENV_VERSION` 两个键，缺少 AppID/Secret；云端运行时仍为 `Nodejs18.15`；不满足正式上架门禁 |
| 2026-08-30-ddf4532 | `ddf4532b2430250994a2a608fc0f4c8f1cc0f934` | 沿用 `index-C7HXq61_.js`；本地与线上 `index.html` SHA-256 均为 `6a02f4288e954d64d161d1fa7b92ad5899796a03ed2231af93623618b3e8233f` | 仅 `admin` 更新代码并发布不可变版本 `5`（2026-08-30 22:01:45）；原始 SCF 别名配置确认版本 `5` 为 `[0,100)`、版本 `4` 为 `[100,100)`；其余 12 个函数未变 | 新建隔离集合 `release_recreated_20260830_213704` 并验证唯一标记可读；未覆盖或读取正式记录；未完成平台备份回档 | `admin` 版本 `4` | Codex | 版本5切流后未知 action 探针符合 `code=-1`、`SESSION_CORRUPTED`、`未知操作`；两个既有环境变量键保留；负责人决定不购买标准版。仍缺 AppID/Secret、四个订阅模板 ID、自有域名安全响应头、告警/RPO/RTO、双平台真机及公众平台材料，不满足完整上架门禁 |

| 2026-08-30-c649be6 | `c649be689698accce554291a77d5e4cf661043dc` | `index-rRvxtOLv.js`；本地与线上 `index.html` SHA-256 `ce50e9c12024523e7d64dd33adbe0c2f2bb8299e04ee31af127751e0570c136e` | 仅 `admin` 更新代码并发布不可变版本 `6`（2026-08-30 22:54:26）；原始 SCF 别名确认版本 `6` `[0,100)`、版本 `5` `[100,100)`；其余 12 个函数未变 | 无；未读取正式记录 | `admin` 版本 `5`；静态托管无平台版本，需从 `3592137` 重新构建并部署 | Codex | `createLoginSession` 探针返回 `SCAN_LOGIN_UNAVAILABLE`，未知 action 保持 `SESSION_CORRUPTED`；线上后台只显示账号密码登录；环境仍为体验版且未购买套餐。小程序订阅授权代码尚未上传，且仍缺 AppID/Secret、四个模板 ID、自有域名安全响应头、告警/RPO/RTO、双平台真机及公众平台材料，不满足完整上架门禁 |

## 七、测试记录

2026-08-30 云端发布审计：目标环境为微信云开发小程序来源的预付费体验版；负责人决定不购买标准版。数据库回档请求被拒绝，已改用新建隔离集合验证，不将其记录为回档成功；13 个函数均保持平台现有 `Nodejs18.15`，本地发布清单已同步；`admin` 版本 `5` 承担 100% 流量，版本 `4` 为回滚点；云函数限频、静态托管防盗链/IP 黑白名单/IP 限频、OPA 自定义策略均未配置；匿名登录已开启，客户端函数调用要求登录态且 `sendReminder` 禁止客户端调用；AppID/Secret 与四个订阅模板变量缺失；默认静态入口命中 `index-C7HXq61_.js`，本地与线上 `index.html` SHA-256 均为 `6a02f4288e954d64d161d1fa7b92ad5899796a03ed2231af93623618b3e8233f`，但未返回所需安全响应头。未读取正式业务记录或凭据值，未购买套餐。

| 日期 | 测试人 | 环境/版本 | 测试范围 | 结果与证据 |
| --- | --- | --- | --- | --- |
| 2026-08-30 | Codex | Git `c649be6`；Node.js 20.19.0 / 26.3.0；CloudBase `yuyue-d0gdy87711d685d64` 体验版 | Strict 本地门禁、云函数发布、静态托管与可视化烟测 | 两个 Node.js 版本均 74/74 测试通过；152 个源码 JavaScript/CJS 与 20 个构建 JavaScript 文件语法通过；141 个 JSON 文件可解析；Node.js 20.19.0 加载 13/13 云函数；13/13 `wx-server-sdk` 锁定 `4.0.2`；`admin-web` `npm ci` 审计 0 漏洞并构建成功；`git diff --check` 通过；`admin` 版本 `6` 100% 流量、版本 `5` 0%；线上入口 `index-rRvxtOLv.js` 且 HTML 哈希与本地一致；未读取正式业务记录或凭据值。 |
| 2026-08-30 | Codex | CloudBase `yuyue-d0gdy87711d685d64`；Git `ddf4532`；`admin` 版本 `5` | 单函数最小发布、不可变版本切流、隔离集合和静态入口一致性 | `admin` 相关安全测试 16/16 通过；锁定 `wx-server-sdk 4.0.2`；仅更新 `admin` 代码，版本5发布完成并由原始 SCF 别名配置确认承载100%流量，版本4保留；切流前后无写入探针均通过；两个既有环境变量键保留；新隔离集合写入/读回成功；本地与线上后台入口资源和 SHA-256 一致；未返回所需安全响应头，且缺少正式凭据、订阅模板和平台验收材料，因此不满足完整上架门禁。 |
| 2026-08-30 | Codex | 本地工作树；Node.js 20.19.0 / 26.3.0 | 扫码创建限流修复、发布手册与最终 Strict 本地门禁 | 两个 Node.js 版本均 72/72 测试通过；171 个项目 JavaScript/CJS 文件语法通过，构建后另复核 20 个 dist JavaScript 文件；141 个 JSON 文件可解析；Node.js 20.19.0 加载 13/13 云函数；13/13 manifest 和锁文件精确锁定 wx-server-sdk 4.0.2；管理后台构建入口为 index-C7HXq61_.js，dist/index.html SHA-256 为 6a02f4288e954d64d161d1fa7b92ad5899796a03ed2231af93623618b3e8233f；git diff --check 通过；两轮只读复核无 P0/P1/P2。CloudBase 登录尚未恢复，本行不证明云端部署、恢复演练或正式审核条件完成。 |
| 2026-08-30 | Codex | 本地提交 `8d34af5`；Node.js 20.19.0 / 26.3.0；CloudBase `admin` 版本 `4` | 最终 Strict 门禁、代码部署、版本路由与正式后台隔离扫码验证 | Node.js 20.19.0 与 26.3.0 均 70/70 测试通过；170 个 JavaScript/CJS 文件语法通过；141 个 JSON 文件可解析；Node.js 20.19.0 加载 13/13 云函数；13/13 `wx-server-sdk` 精确锁定 `4.0.2`；管理后台生产构建通过；`git diff --check` 通过；`admin` 版本 `4` 已发布，`$LATEST` 100%；扫码入口仍受缺失 AppID/Secret 与运行时迁移阻塞；未读取业务记录、函数日志、Cookie、本地存储或凭据值。 |
| 2026-08-30 | Codex | CloudBase `yuyue-d0gdy87711d685d64`；发布批次 `31f6954` | 云函数分阶段部署、回滚验证、管理后台静态托管与隔离匿名金丝雀 | `admin`、`login`、`createAppointment` 部署成功并发布版本 `3`，版本 `2` 可回滚；线上入口为 `index-C7HXq61_.js`；登录页正常且原始云端错误未暴露；扫码会话已进入小程序码生成阶段，但新旧函数版本均生成失败，确认是既有配置或权限缺口；未读取业务记录、Cookie、本地存储或凭据。 |
| 2026-08-30 | Codex | 发布提交前本地工作树；Node.js 20.19.0 / 26.3.0 | 最终 Strict 本地门禁 | 68/68 测试通过；147 个 JavaScript/CJS 文件语法通过；141 个 JSON 文件可解析；Node.js 20.19.0 加载 13/13 云函数；13/13 `wx-server-sdk` manifest、根锁定项和安装版本均为 `4.0.2`；管理后台生产构建通过，入口为 `index-C7HXq61_.js`，`dist/index.html` SHA-256 为 `6a02f4288e954d64d161d1fa7b92ad5899796a03ed2231af93623618b3e8233f`。 |
| 2026-08-30 | Codex | 本地工作树；微信开发者工具 Stable 2.02.2608040 | 自动化、生产构建、开发者工具冒烟 | 33/33 测试；40 个业务 JS；74 个 JSON；13 个发布函数锁文件；管理后台构建通过；当前错误 0、问题 0；2026-08-31 显示 14 个可约时段；未创建预约 |
| 2026-08-30 | Codex | 本地工作树；Node v26.3.0；当时云函数目标运行时 Node 18.15 | Strict 本地门禁与依赖审计 | 37/37 测试；139 个 JS 语法通过；141 个 JSON 可解析；13 个发布函数锁文件一致；管理后台生产构建通过；管理后台与小程序 npm audit 均为 0；云函数 wx-server-sdk 2.6.3 审计 17 项（3 严重、7 高、7 中），4.0.2 临时验证降至 6 项（5 高、1 中）且 Node 18.15 加载 13/13；当时正式迁移待明确授权，后续结果见下方 `Nodejs20.19` 记录 |
| 2026-08-30 | Codex | 公开静态托管；未登录 | 登录页与资源版本核验 | 页面可正常加载且无控制台警告或错误；线上资源为 `index-D_ZREcPg.js`、`Login-BvdqRuWs.js`，本地最新构建为 `index-BIOFdWwT.js`、`Login-Bm9WqxwM.js`，确认线上仍不是当前构建；未读取业务数据 |
| 2026-08-30 | Codex | 公开静态托管；未登录 | HTTP 响应头核验 | HTML 为 `max-age=120`、哈希资源为 `max-age=31536000`；未返回 CSP、HSTS、`X-Content-Type-Options`、`Referrer-Policy`、`Permissions-Policy` 或防嵌入响应头，生产域名与网关安全头门禁未完成 |
| 2026-08-30 | Codex | 本地工作树；现有锁文件 | 可复现安装与构建 | `admin-web npm ci --ignore-scripts` 后依赖树完整，生产构建再次通过且入口资源哈希保持 `index-BIOFdWwT.js`；`miniprogram npm ci --ignore-scripts` 成功并审计 0 项 |
| 2026-08-30 | Codex | 本地工作树；CloudBase 目标运行时 `Nodejs20.19` | 运行时迁移兼容性 | 根清单 13 个函数及 `admin` 嵌套清单已统一迁移；使用 Node.js 20.19.0 与锁定的 `wx-server-sdk` 2.6.3 加载 13/13 云函数成功；回滚需同时恢复两份 `cloudbaserc.json` 并部署上一云函数版本 |
| 2026-08-30 | Codex | 本地工作树；未部署 | 安全收口后的 Strict 本地门禁 | 42/42 测试通过；142 个 JavaScript 文件语法通过；141 个 JSON 文件可解析；Node.js 20.19.0 加载 13/13 云函数；管理后台生产构建通过，入口仍为 `index-BIOFdWwT.js`；`git diff --check` 通过 |
| 2026-08-30 | Codex | `/private/tmp` 隔离迁移；未修改正式锁文件 | `wx-server-sdk 4.0.2` 迁移预演 | 13/13 manifest 与锁文件生成精确 `4.0.2`，13/13 各自离线安装并在 Node.js 20.19.0 加载成功；10 个 SDK API、25 个数据库 API 表面检查无缺失；全套 42/42 测试在 Node.js 20.19.0 通过；依赖审计仍为 5 个高危、1 个中危，正式迁移与风险接受待授权 |
| 2026-08-30 | Codex | 本地工作树；正式依赖迁移 | `wx-server-sdk 4.0.2` 锁定与风险接受 | 13/13 manifest 和锁文件精确锁定 `4.0.2`，逐函数执行 `npm ci --ignore-scripts` 成功；每个函数审计仍为 5 个高危、1 个中危，已由项目负责人限时接受至 2026-11-28，不表示漏洞已修复；完整 Strict 门禁见后续发布记录 |

## 2026-08-31 免费体验版发布证据

- [x] 本地发布提交为 `b5b0997`，未推送；`AGENTS.md`、`CLAUDE.md` 未纳入提交。
- [x] 13/13 云函数 manifest、根锁定项和安装版本均为 `wx-server-sdk 4.0.2`；限时接受的 5 个高危、1 个中危上游风险仍有效至 2026-11-28。
- [x] `admin` 版本 `8` 为 `[0,100)`，版本 `6` 为 `[100,100)`；`getServices` 版本 `3` 为 `[0,100)`，版本 `2` 为 `[100,100)`。
- [x] `createAppointment` 版本 `5` 为 `[0,100)`，版本 `4` 为 `[100,100)`；`sendReminder` 版本 `3` 为 `[0,100)`，版本 `2` 为 `[100,100)`。均以原始 SCF 别名配置核验，未采用 CloudBase CLI 3.5.8 的反向汇总显示。
- [x] 已创建公开只读 `/admin-health` 路由；`GET ?action=health` 返回 `200` 和 `no-store`、CSP、`Referrer-Policy`、`X-Content-Type-Options`、`X-Frame-Options`，且不读取数据库。
- [x] 管理后台生产构建和静态托管部署成功，线上入口为 `index-CGuRvXFB.js`；本地与线上主入口 SHA-256 均为 `6e13589ed64a805c4232dd410a8d8b94e2c429a7464656e69a4e457d3d75c00d`，`dist/index.html` SHA-256 为 `6ac00d6b45d5257ac02ae8a64e10f981959503ef00185088`。
- [x] 部署前静态产物保存在 `/tmp/yxt-admin-before-b5b0997.E8u541`，仅供本次会话内紧急重传；静态托管本身无版本管理。
- [x] 无正式数据探针通过：`admin.health` 正常；空参数调用 `createAppointment` 被登录门禁拒绝；空参数调用 `sendReminder` 被定时触发器门禁拒绝；未调用会读取正式服务记录的 `getServices`。
- [x] 本次未购买套餐、未创建订单、未付款，也未开启要求付费确认的日志功能。
- [ ] 静态托管 HTML 仍只有 CSP meta，服务器响应仍缺 HSTS、`X-Content-Type-Options`、`Referrer-Policy`、`Permissions-Policy` 和防嵌入响应头。
- [ ] GitHub 监控工作流因禁止推送尚未生效；付费日志、告警送达、独立备份恢复、RPO/RTO、双平台真机和公众平台材料仍无完成证据。

## 2026-08-31 免费版安全入口候选（未部署）

- [x] 不购买标准版；本地已生成函数入口 shell 和静态根页跳转，未新增云函数或依赖。
- [x] 管理后台生产构建通过；`/admin` shell 使用绝对 `/assets/...` 资源路径，兼容 `/admin` 与 `/admin/`。
- [x] HSTS 三态白名单和自动化证明已完成：首发 `300` 秒 canary、最终 `31536000`、回滚 `0`；线上仍须按顺序实际验证。
- [x] 当前源码状态管理后台生产构建通过，本地全套 95/95 测试通过；监控工作流 YAML 解析、制品引用完整性和 `git diff --check` 通过。
- [x] 已创建当前任务内每 15 分钟运行的本地公开健康检查；不读取正式记录、日志、凭据或数据库。
- [ ] 登录 CloudBase 后先导出完整 HTTP 路由快照，并验证免费体验版是否允许静态路由原生添加响应头；可用时优先采用原生能力并删除函数回退。
- [ ] 原生响应头不可用时，部署 `/admin` → `SCF/admin`、`/assets` 和 `/logo-business.png` → `STATIC_STORE/staticstore`，保留 `/admin-health`，并让根静态页跳转到 `/admin`。
- [ ] 用公开无状态探针验证 `/admin`、`/admin/`、一个 JS、一个 CSS、`/logo-business.png`、匿名登录和完整服务器安全响应头；任一失败即回滚。
- [ ] 当前结论：代码、四个函数路由、健康探针和管理后台已发布，但仍不满足完整上架门禁，不得据此直接提交审核。
